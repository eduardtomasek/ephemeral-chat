{
  "name": "ephemeral-chat",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "disclaimer": "Educational project for testing and learning about end-to-end encrypted communication. Provided as-is and used at your own risk. Must be used only in compliance with applicable laws. Not intended for illegal activity.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/cli.js",
    "dev": "tsx watch src/cli.ts",
    "test": "node --import tsx --test --test-concurrency=1 test/*.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
